<%= form_for(@product_order) do |f| %>
  <% if @product_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_order.errors.count, "error") %> prohibited this product_order from being saved:</h2>

      <ul>
      <% @product_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sku %><br>
    <%= f.text_field :sku %>
  </div>
  <div class="field">
    <%= f.label :cantidad %><br>
    <%= f.number_field :cantidad %>
  </div>
  <div class="field">
    <%= f.label :disponibe %><br>
    <%= f.datetime_select :disponibe %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
