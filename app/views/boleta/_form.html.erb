<%= form_for(@boletum) do |f| %>
  <% if @boletum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boletum.errors.count, "error") %> prohibited this boletum from being saved:</h2>

      <ul>
      <% @boletum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :boleta_id %><br>
    <%= f.text_field :boleta_id %>
  </div>
  <div class="field">
    <%= f.label :orden_id %><br>
    <%= f.text_field :orden_id %>
  </div>
  <div class="field">
    <%= f.label :estado %><br>
    <%= f.text_field :estado %>
  </div>
  <div class="field">
    <%= f.label :total %><br>
    <%= f.number_field :total %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
